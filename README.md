# agena
A terminal-based Gemini protocol client written in C

### INSTALL

Clone the repo:

```
git clone https://github.com/aschuhardt/agena.git
cd agena
```

Build:

```
mkdir build
cd build
cmake ..
make
```
